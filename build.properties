project.name = java2as
src.dir = src
test.dir = test
dist.dir = dist
build.dir = ${dist.dir}/build
src.build.dir = ${build.dir}/src
test.build.dir = ${build.dir}/test
lib.dir = lib
env_lib.dir = env_lib
src.config.dir = etc
deploy_config.dir = deploy
checkstyle.report.dir = ${dist.dir}/checkstyle
test.report.dir = ${dist.dir}/testreport
cmd.dir =  ${dist.dir}/cmd
bat.dir = bat

main.version = 1
minor.version = 0